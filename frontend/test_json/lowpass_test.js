
// this.noise_player.generateLowpassFilter();

// this.nodes["source"].connect(this.noise_player.lowpass_filter);

// this.noise_player.lowpass_filter.connect(this.nodes["gain"]);

// this.nodes["source"].start();


// 結果
/*

クリックノイズは時刻同期のズレではなく、フィルター自体のズレにあると考えられる

 */